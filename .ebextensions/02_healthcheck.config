option_settings:
  aws:elasticbeanstalk:environment:process:default:
    HealthCheckPath: /health
    # 308 kept during rollout while FORCE_HTTPS still redirects old builds.
    MatcherHTTPCode: "200,308"
